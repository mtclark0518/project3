import { Provider, NgModuleFactory, Type } from '@angular/core';
export interface NgSetupOptions {
    aot?: boolean;
    bootstrap: Type<{}> | NgModuleFactory<{}>;
    providers?: Provider[];
}
